<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WindowsApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:WindowsApp.Controls"
    xmlns:local="using:LocalFile.Models"
    mc:Ignorable="d"
    Title="FLex Search">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop/>
    </Window.SystemBackdrop>

    <Grid x:Name="root" RequestedTheme="Default">
        
        <Grid.Resources>

            <DataTemplate x:Key="DefaultSearchList" x:DataType="local:Record">
                <StackPanel Grid.Column="1"
                                    Orientation="Horizontal">

                    <SymbolIcon Symbol="AllApps"
                                            Margin="0,0,10,0"/>
                    <TextBlock Grid.Column="1"
                                        Text="{x:Bind Name}"
                                           FontWeight="SemiBold"
                                           FontSize="14"
                                           Margin="0,0,10,0"/>
                    <TextBlock
                                    Text="{x:Bind Size}" 
                                    Margin="0,2,0,0"
                                    FontSize="12"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock
                                    Text=" lines"
                                    Margin="0,2,0,0"
                                    FontSize="12"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </DataTemplate>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Background="#26000000"
                Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.Resources>
                <ResourceDictionary>
                    <ResourceDictionary.MergedDictionaries>
                        <ResourceDictionary Source="/Resources/Styles.xaml"/>
                    </ResourceDictionary.MergedDictionaries>
                </ResourceDictionary>
            </Grid.Resources>

            <StackPanel Grid.Row="0">
                <TextBlock Margin="15,20,20,15"
                        FontSize="20"
                        FontWeight="Bold"
                           Foreground="{StaticResource FontColorBrushOrange}"
                        Text="Logs"/>
            </StackPanel>

            <StackPanel Grid.Row="1">
                <ScrollViewer Height="366"
                        ZoomMode="Enabled"
                              HorizontalScrollMode="Enabled"
                        HorizontalScrollBarVisibility="Auto"
                              VerticalScrollMode="Enabled"
                        VerticalScrollBarVisibility="Auto">

                    <ListView
                        Width="Auto"
                            ItemsSource="{x:Bind ViewModel.loog, Mode=TwoWay}">

                    </ListView>
                </ScrollViewer>
            </StackPanel>
        </Grid>

        <Grid  Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Padding="30,30,30,0"
                    Grid.Column="1"
                    Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>


                <StackPanel Grid.Column="2"
                        VerticalAlignment="Center"
                        Padding="0,0,0,4">
                    <TextBlock x:Name="txtCaminhoArquivoas"
                            FontSize="20"
                            Foreground="{ThemeResource FontColorBrush}"
                            FontWeight="Bold"
                            Text="{x:Bind ViewModel.frontText.ProjectName , Mode=OneWay}"/>
                    <TextBlock TextWrapping="Wrap"
                            Width="380"
                            x:Name="txtCaminhoArquivo"
                            Text="{x:Bind ViewModel.frontText.ProjectVersion, Mode=OneWay}"/>
                </StackPanel>

                <Button Height="60" Padding="0" Grid.Column="0" Click="btnTottleTheme_click">
                    <Image Source="/Assets/android-chrome-512x512.png"
                       />
                </Button>
                

            </Grid>

            <Grid ColumnSpacing="20"
                    RowSpacing="5"
                    Padding="30,30,30,00"
                    Grid.Column="1"
                    Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid.Resources>
                    <Style x:Key="BaseSubTextBoxStyle" TargetType="TextBlock">
                        <Setter Property="FontSize" Value="11"/>
                    </Style>
                    <Style x:Key="SubTextBoxStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseSubTextBoxStyle}">
                        <Setter Property="Padding" Value="10,0,0,00"/>
                        <Setter Property="Foreground" Value="#666"/>
                    </Style>
                </Grid.Resources>

                <NumberBox
                    x:Name="nbHowManySearch"
                    Header="{x:Bind ViewModel.frontText.HowManySearch}"
                    Value="{x:Bind ViewModel.HowManySeacrh, Mode=TwoWay}"
                    Grid.Column="0"
                    Grid.Row="0"
                    SpinButtonPlacementMode="Inline"
                    PlaceholderText="Put a number"
                    SmallChange="1"
                    ValueChanged="{x:Bind ViewModel.HowManySearchSubTextUpdate}"
                    LargeChange="100"/>

                <TextBlock Style="{StaticResource SubTextBoxStyle}"
                        Grid.Row="1"
                        Grid.Column="0"
                        x:Name="txtHowManySearch"
                        Text="{x:Bind ViewModel.frontText.HowManySearchSubText, Mode=OneWay}"/>

                <NumberBox
                    x:Name="nbTimeBetweenSearch"
                    Header="{x:Bind ViewModel.frontText.HowLong}"
                    Value="{x:Bind ViewModel.HowLongTime, Mode=TwoWay}"
                    Grid.Column="1"
                    Grid.Row="0"
                    SpinButtonPlacementMode="Inline"
                    ValueChanged="{x:Bind ViewModel.HowLongSubTextUpdate}"
                    SmallChange="30"
                    LargeChange="100"/>

                <TextBlock Style="{StaticResource SubTextBoxStyle}"
                        Grid.Row="1"
                        Grid.Column="1"
                        x:Name="txtTimeBetweenSearch"
                        Text="{x:Bind ViewModel.frontText.HowLongSubText, Mode=OneWay}"/>

            </Grid>

            <Grid Padding="30,30,30,00"
                    Grid.Column="1"
                    Grid.Row="2">


                <ComboBox x:Name="cmbSearchList"
                          Header="{x:Bind ViewModel.frontText.ListOfSearchOption}"
                          PlaceholderText="Selecione a lista"
                          HorizontalAlignment="Stretch"
                          SelectedValue="{x:Bind ViewModel.SelectedSearchListOption, Mode=TwoWay}"
                          ItemsSource="{x:Bind ViewModel.searchListOption, Mode=OneWay}"
                          ItemTemplate="{StaticResource DefaultSearchList}"
                          />



            </Grid>

            <Grid Padding="30,30,30,00"
                    Grid.Column="1"
                    Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal"
                        Grid.Column="0">
                    <ToggleSwitch x:Name="tgsTurnOffComputer"
                            Header="{x:Bind ViewModel.frontText.TurnOfComputer.mainDescription}"
                            IsOn="{x:Bind ViewModel.TurnOffComputer, Mode=TwoWay}"
                            OffContent="{x:Bind ViewModel.frontText.TurnOfComputer.turnOn}"
                            OnContent="{x:Bind ViewModel.frontText.TurnOfComputer.turnOff}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal"
                        Grid.Column="3">
                    <ToggleSwitch x:Name="tgsUpdatePage"
                            Header="{x:Bind ViewModel.frontText.UpdateThePage.mainDescription}"
                            OffContent="{x:Bind ViewModel.frontText.UpdateThePage.turnOn}"
                            OnContent="{x:Bind ViewModel.frontText.UpdateThePage.turnOff}"
                            IsOn="True"/>
                </StackPanel>


            </Grid>

            <Grid Padding="30,20,30,0"
                    Grid.Column="1"
                    Grid.Row="4">
                <Button
                    x:Name="btnExecutar"
                    HorizontalAlignment="Stretch"
                    Style="{StaticResource AccentButtonStyle}"
                    Command="{x:Bind ViewModel.AddCommand, Mode=OneTime}"
                    >
                    <StackPanel Orientation="Horizontal"
                            Spacing="8">
                        <TextBlock Text="{x:Bind ViewModel.Status, Mode=OneWay}"
                                VerticalAlignment="Center"/>
                        <SymbolIcon x:Name="sbiButtonIco"
                                Symbol="Find"/>
                    </StackPanel>
                </Button>
            </Grid>

        </Grid>



    </Grid>

</Window>
